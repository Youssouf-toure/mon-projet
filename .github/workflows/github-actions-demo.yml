name: GitHub Actions Demo

on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo "🎉 Bonjour, vous utilisez GitHub Actions !"
